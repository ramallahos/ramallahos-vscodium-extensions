post_install() {
for i in $( curl https://raw.githubusercontent.com/ramallahos/ramallahos-vscodium-extensions/main/extensions );
do
    echo "USER:      $USER"
    echo "SUDO_USER: $SUDO_USER"
    
    # sudo su -c "vscodium  --install-extension $i" -s /bin/bash 
done
}
